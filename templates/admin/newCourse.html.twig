{% block body %}
    <h3 class="display-5 my-4">Ajout d'une formation</h3>
    <div class="offset-2">
        {{ form_start(formNewCourse) }}
        {{ form_widget(formNewCourse) }}
        <input type="submit" value="Ajouter" class="btn btn-outline-dark">
        {{ form_end(formNewCourse) }}
    </div>
{% endblock %}
