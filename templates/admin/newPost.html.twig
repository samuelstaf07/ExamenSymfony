{% block body %}
    <h3 class="display-5 my-4">Ajout d'une actualité</h3>
    <div class="offset-2">
        {{ form_start(formNewPost) }}
        {{ form_widget(formNewPost) }}
        <input type="submit" value="Ajouter" class="btn btn-outline-dark">
        {{ form_end(formNewPost) }}
    </div>
{% endblock %}
